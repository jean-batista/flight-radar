name: Backend CD (Docker Hub)

on:
  push:
    branches: 
        - "main"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
    - name: Setup Java 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build Application .jar
      working-directory: ./backend
      run: mvn clean package -DskipTests

    - name: Docker Compose Build
      working-directory: .
      run: docker compose build

    - name: Push to Docker Hub Container Registry
      run: |
        docker tag ${{ secrets.DOCKER_USERNAME }}/flight_radar_backend:latest ${{ secrets.DOCKER_USERNAME }}/flight_radar_backend:v${{ github.run_number }}
        
        docker push ${{ secrets.DOCKER_USERNAME }}/flight_radar_backend:latest
        docker push ${{ secrets.DOCKER_USERNAME }}/flight_radar_backend:v${{ github.run_number }}
